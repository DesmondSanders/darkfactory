{"summary":"Implemented a runnable Node.js FDE SDLC control-plane MVP with enterprise-focused security and governance fixes from QA feedback: strict OIDC secret handling (no insecure default outside dev), policy engine with Suggest/HIL/HOL autonomy, separation-of-duties and environment restrictions enforced at execution/reversal time, immutable hash-chain audit with verification/export/retention, graceful model-provider degradation with fallback, async job orchestration, release-readiness quality gates, KPI tracking, role dashboards, and adapter-based integrations for Jira/Azure DevOps, GitHub/GitLab, CI, ServiceNow, Slack/Teams, and observability. Added comprehensive tests covering autonomy modes, approvals, SoD, quality gates, audit integrity, and reliability fallback.","files":[{"path":"package.json","content":"{\n  \"name\": \"fde-sdlc-control-plane\",\n  \"version\": \"0.4.0\",\n  \"private\": true,\n  \"type\": \"module\",\n  \"scripts\": {\n    \"start\": \"node src/server.js\",\n    \"test\": \"node --test\"\n  }\n}"},{"path":"src/server.js","content":"import http from 'node:http';\nimport { URL } from 'node:url';\nimport { randomUUID, createHash, createHmac } from 'node:crypto';\nimport fs from 'node:fs';\nimport path from 'node:path';\n\nconst PORT = Number(process.env.PORT || 3000);\nconst NODE_ENV = process.env.NODE_ENV || 'development';\nconst DATA_DIR = process.env.DATA_DIR || path.join(process.cwd(), '.data');\nconst AUDIT_FILE = path.join(DATA_DIR, 'audit.jsonl');\nconst STATE_FILE = path.join(DATA_DIR, 'state.json');\nconst RETENTION_DAYS = Number(process.env.AUDIT_RETENTION_DAYS || 365);\n\nfs.mkdirSync(DATA_DIR, { recursive: true });\nif (!fs.existsSync(AUDIT_FILE)) fs.writeFileSync(AUDIT_FILE, '');\n\nconst config = {\n  autonomy: {\n    level: process.env.AUTONOMY_LEVEL || 'HIL', // SUGGEST_ONLY | HIL | HOL\n    requireApprovalFor: ['production_change', 'merge_main', 'release']\n  },\n  oidc: {\n    issuer: process.env.OIDC_ISSUER || 'https://issuer.example.com',\n    audience: process.env.OIDC_AUDIENCE || 'fde-sdlc-control-plane',\n    secret: process.env.OIDC_HS256_SECRET || ''\n  },\n  security: {\n    enforceTlsHeader: process.env.ENFORCE_TLS_HEADER === 'true',\n    tenantIsolation: true\n  },\n  model: {\n    provider: process.env.MODEL_PROVIDER || 'managed-default',\n    timeoutMs: Number(process.env.MODEL_TIMEOUT_MS || 4000),\n    fallbackEnabled: process.env.MODEL_FALLBACK_ENABLED !== 'false'\n  },\n  pilot: {\n    team: process.env.PILOT_TEAM || 'team-alpha'\n  }\n};\n\nif (NODE_ENV !== 'development' && !config.oidc.secret) {\n  throw new Error('OIDC_HS256_SECRET is required outside development');\n}\nif (NODE_ENV === 'development' && !config.oidc.secret) {\n  config.oidc.secret = 'dev-only-secret-change-me';\n}\n\nconst state = loadState();\n\nfunction loadState() {\n  if (!fs.existsSync(STATE_FILE)) {\n    const initial = {\n      initiatives: {},\n      actions: {},\n      approvals: {},\n      jobs: {},\n      kpis: {\n        baseline: { leadTimeDays: 10, deploysPerWeek: 4, sev12Incidents: 1 },\n        current: { leadTimeDays: 8, deploysPerWeek: 5, sev12Incidents: 1 }\n      },\n      metrics: {\n        autonomyAdoption: { SUGGEST_ONLY: 0, HIL: 0, HOL: 0 },\n        bottlenecks: [],\n        risks: []\n      }\n    };\n    fs.writeFileSync(STATE_FILE, JSON.stringify(initial, null, 2));\n    return initial;\n  }\n  return JSON.parse(fs.readFileSync(STATE_FILE, 'utf8'));\n}\n\nfunction saveState() {\n  fs.writeFileSync(STATE_FILE, JSON.stringify(state, null, 2));\n}\n\nfunction json(res, status, body) {\n  res.writeHead(status, { 'content-type': 'application/json' });\n  res.end(JSON.stringify(body));\n}\n\nfunction parseBody(req) {\n  return new Promise((resolve, reject) => {\n    let data = '';\n    req.on('data', (chunk) => {\n      data += chunk;\n      if (data.length > 2_000_000) {\n        reject(new Error('Payload too large'));\n        req.destroy();\n      }\n    });\n    req.on('end', () => {\n      if (!data) return resolve({});\n      try {\n        resolve(JSON.parse(data));\n      } catch {\n        reject(new Error('Invalid JSON'));\n      }\n    });\n    req.on('error', reject);\n  });\n}\n\nfunction b64urlDecode(input) {\n  const pad = '='.repeat((4 - (input.length % 4)) % 4);\n  const base64 = (input + pad).replace(/-/g, '+').replace(/_/g, '/');\n  return Buffer.from(base64, 'base64').toString('utf8');\n}\n\nfunction verifyJwtHS256(token, secret) {\n  const parts = token.split('.');\n  if (parts.length !== 3) throw new Error('Malformed token');\n  const [h, p, s] = parts;\n  const header = JSON.parse(b64urlDecode(h));\n  const payload = JSON.parse(b64urlDecode(p));\n  if (header.alg !== 'HS256') throw new Error('Unsupported alg');\n  const expected = createHmac('sha256', secret).update(`${h}.${p}`).digest('base64url');\n  if (expected !== s) throw new Error('Bad signature');\n  const now = Math.floor(Date.now() / 1000);\n  if (payload.exp && now > payload.exp) throw new Error('Expired token');\n  if (payload.iss !== config.oidc.issuer) throw new Error('Bad issuer');\n  const aud = payload.aud;\n  const audOk = Array.isArray(aud) ? aud.includes(config.oidc.audience) : aud === config.oidc.audience;\n  if (!audOk) throw new Error('Bad audience');\n  return payload;\n}\n\nfunction getAuth(req) {\n  const auth = req.headers.authorization || '';\n  if (!auth.startsWith('Bearer ')) return null;\n  const token = auth.slice(7);\n  try {\n    const claims = verifyJwtHS256(token, config.oidc.secret);\n    return {\n      sub: claims.sub,\n      name: claims.name || claims.sub,\n      roles: claims.roles || [],\n      scopes: claims.scopes || [],\n      tenant: claims.tenant || 'default'\n    };\n  } catch {\n    return null;\n  }\n}\n\nfunction requireAuth(req, res) {\n  const user = getAuth(req);\n  if (!user) {\n    json(res, 401, { error: 'Unauthorized' });\n    return null;\n  }\n  return user;\n}\n\nfunction requireScope(user, scope) {\n  return user.scopes.includes(scope) || user.roles.includes('admin');\n}\n\nfunction enforceTenant(user, resourceTenant) {\n  return user.roles.includes('admin') || user.tenant === resourceTenant;\n}\n\nfunction nowIso() {\n  return new Date().toISOString();\n}\n\nfunction readAuditLines() {\n  const raw = fs.readFileSync(AUDIT_FILE, 'utf8').trim();\n  if (!raw) return [];\n  return raw.split('\\n').filter(Boolean).map((l) => JSON.parse(l));\n}\n\nfunction appendAudit(entry) {\n  const lines = readAuditLines();\n  const prevHash = lines.length ? lines[lines.length - 1].hash : 'GENESIS';\n  const payload = { ...entry, prevHash };\n  const hash = createHash('sha256').update(JSON.stringify(payload)).digest('hex');\n  const record = { ...payload, hash };\n  fs.appendFileSync(AUDIT_FILE, JSON.stringify(record) + '\\n');\n  return record;\n}\n\nfunction verifyAuditChain() {\n  const lines = readAuditLines();\n  let prev = 'GENESIS';\n  for (const rec of lines) {\n    if (rec.prevHash !== prev) return { ok: false, error: 'Broken prevHash link', at: rec.id };\n    const { hash, ...payload } = rec;\n    const expected = createHash('sha256').update(JSON.stringify(payload)).digest('hex');\n    if (expected !== hash) return { ok: false, error: 'Hash mismatch', at: rec.id };\n    prev = hash;\n  }\n  return { ok: true, count: lines.length, head: prev };\n}\n\nfunction applyRetention() {\n  const cutoff = Date.now() - RETENTION_DAYS * 24 * 60 * 60 * 1000;\n  const kept = readAuditLines().filter((r) => new Date(r.when).getTime() >= cutoff);\n  fs.writeFileSync(AUDIT_FILE, kept.map((r) => JSON.stringify(r)).join(kept.length ? '\\n' : ''));\n  if (kept.length) fs.appendFileSync(AUDIT_FILE, '\\n');\n  return { retained: kept.length, retentionDays: RETENTION_DAYS };\n}\n\nfunction modelGenerate(prompt) {\n  const unavailable = process.env.MODEL_PROVIDER_UNAVAILABLE === 'true';\n  if (unavailable) throw new Error('Model provider unavailable');\n  return {\n    rationale: 'Generated by managed model runtime',\n    confidence: 0.82,\n    sourceContext: ['initiative_text', 'policy_defaults'],\n    policyRefs: ['AUTONOMY-1', 'GOV-APPROVAL-1'],\n    output: prompt\n  };\n}\n\nfunction fallbackGenerate(prompt) {\n  return {\n    rationale: 'Fallback deterministic planner used due to provider outage',\n    confidence: 0.55,\n    sourceContext: ['initiative_text'],\n    policyRefs: ['RELIABILITY-DEGRADE-1'],\n    output: prompt\n  };\n}\n\nfunction decomposeInitiative(text) {\n  const base = {\n    epics: [{ title: 'Control Plane Foundation', description: text }],\n    stories: [\n      { title: 'As a PM, I submit initiative in natural language', acceptance: ['Initiative accepted', 'Backlog generated'] },\n      { title: 'As an engineer, I review agent plan', acceptance: ['Plan includes tasks and tests'] }\n    ],\n    tasks: [\n      { title: 'Create branch and draft PR', type: 'implementation' },\n      { title: 'Generate unit/integration/regression tests', type: 'testing' },\n      { title: 'Run release readiness gates', type: 'release' }\n    ]\n  };\n  return base;\n}\n\nconst adapters = {\n  work: {\n    syncBacklog: async ({ provider, initiativeId, backlog }) => ({ provider, initiativeId, synced: true, items: backlog.stories.length + backlog.tasks.length })\n  },\n  scm: {\n    createBranch: async ({ repo, branch }) => ({ repo, branch, created: true }),\n    draftPr: async ({ repo, branch, title }) => ({ repo, branch, title, prUrl: `https://example.com/${repo}/pull/${Math.floor(Math.random() * 1000)}` })\n  },\n  ci: {\n    publishTests: async ({ pipeline, tests }) => ({ pipeline, published: true, count: tests.length }),\n    run: async () => ({ passRate: 0.97, securityScan: 'pass', riskScore: 0.22, approvals: true })\n  },\n  itsm: {\n    createChange: async ({ actionId }) => ({ changeId: `CHG${Date.now()}`, actionId })\n  },\n  collab: {\n    notify: async ({ channel, message }) => ({ channel, delivered: true, message })\n  },\n  observability: {\n    postReleaseSignals: async () => ({ errorRate: 0.01, latencyP95: 220, incidentsSev12: 0 })\n  }\n};\n\nfunction evaluateReadiness(gates, run) {\n  const result = {\n    pass: run.passRate >= (gates.minPassRate ?? 0.95) &&\n      run.securityScan === (gates.securityScanRequired ?? 'pass') &&\n      run.riskScore <= (gates.maxRiskScore ?? 0.3) &&\n      (!!run.approvals === !!(gates.requireApprovals ?? true)),\n    details: {\n      passRate: run.passRate,\n      securityScan: run.securityScan,\n      riskScore: run.riskScore,\n      approvals: run.approvals\n    }\n  };\n  return result;\n}\n\nfunction requiresApproval(actionType, env) {\n  if (env === 'production') return true;\n  return config.autonomy.requireApprovalFor.includes(actionType);\n}\n\nfunction enforceExecutionPolicy(action, user, operation) {\n  if (action.environment === 'production' && !user.roles.includes('release-manager') && !user.roles.includes('admin')) {\n    return 'Production actions require release-manager role';\n  }\n  if (operation === 'execute' && action.createdBy === user.sub) {\n    return 'Separation of duties: planner cannot execute own action';\n  }\n  if (operation === 'reverse' && action.executedBy && action.executedBy === user.sub) {\n    return 'Separation of duties: executor cannot reverse own action';\n  }\n  if (action.environment === 'production' && !action.approved) {\n    return 'Production action requires explicit approval';\n  }\n  return null;\n}\n\nfunction createJob(type, payload, handler) {\n  const id = randomUUID();\n  state.jobs[id] = { id, type, status: 'queued', createdAt: nowIso(), payload, result: null, error: null };\n  saveState();\n  setTimeout(async () => {\n    state.jobs[id].status = 'running';\n    saveState();\n    try {\n      const result = await handler(payload);\n      state.jobs[id].status = 'completed';\n      state.jobs[id].result = result;\n      saveState();\n    } catch (e) {\n      state.jobs[id].status = 'failed';\n      state.jobs[id].error = e.message;\n      saveState();\n    }\n  }, 10);\n  return state.jobs[id];\n}\n\nconst server = http.createServer(async (req, res) => {\n  try {\n    if (config.security.enforceTlsHeader && req.headers['x-forwarded-proto'] !== 'https') {\n      return json(res, 400, { error: 'TLS required' });\n    }\n\n    const url = new URL(req.url, `http://${req.headers.host}`);\n\n    if (req.method === 'GET' && url.pathname === '/health') {\n      return json(res, 200, { ok: true, service: 'fde-sdlc-control-plane', time: nowIso() });\n    }\n\n    if (req.method === 'POST' && url.pathname === '/initiatives') {\n      const user = requireAuth(req, res); if (!user) return;\n      if (!requireScope(user, 'initiative:write')) return json(res, 403, { error: 'Forbidden' });\n      const body = await parseBody(req);\n      const id = randomUUID();\n      const text = body.text || '';\n      let model;\n      try {\n        model = modelGenerate(text);\n      } catch {\n        if (!config.model.fallbackEnabled) return json(res, 503, { error: 'Model unavailable' });\n        model = fallbackGenerate(text);\n      }\n      const backlog = decomposeInitiative(text);\n      const sync = await adapters.work.syncBacklog({ provider: body.workProvider || 'jira', initiativeId: id, backlog });\n      state.initiatives[id] = { id, tenant: user.tenant, text, backlog, explainability: model, sync, createdBy: user.sub, createdAt: nowIso() };\n      saveState();\n      appendAudit({ id: randomUUID(), who: user.sub, what: 'initiative.create', when: nowIso(), why: 'decompose and sync backlog', evidence: { initiativeId: id, sync }, reversible: false, tenant: user.tenant });\n      return json(res, 201, state.initiatives[id]);\n    }\n\n    if (req.method === 'POST' && url.pathname === '/actions/plan') {\n      const user = requireAuth(req, res); if (!user) return;\n      if (!requireScope(user, 'action:write')) return json(res, 403, { error: 'Forbidden' });\n      const body = await parseBody(req);\n      const actionId = randomUUID();\n      const action = {\n        id: actionId,\n        tenant: user.tenant,\n        type: body.type || 'merge_main',\n        environment: body.environment ||