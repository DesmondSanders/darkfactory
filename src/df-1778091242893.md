{"summary":"Implemented a runnable Node.js control-plane MVP that satisfies the FDE SDLC acceptance criteria with concrete endpoints, adapter-based enterprise integrations, policy-driven autonomy, immutable audit verification/export, RBAC with OIDC JWT validation, async orchestration, role dashboards, KPI tracking, and graceful model-provider degradation. Includes tests for core flows and guardrails.","files":[{"path":"package.json","content":"{\n  \"name\": \"fde-sdlc-control-plane\",\n  \"version\": \"0.3.0\",\n  \"private\": true,\n  \"type\": \"module\",\n  \"scripts\": {\n    \"start\": \"node src/server.js\",\n    \"test\": \"node --test\"\n  }\n}"},{"path":"src/server.js","content":"import http from 'node:http';\nimport { URL } from 'node:url';\nimport { randomUUID, createHash, createVerify } from 'node:crypto';\nimport fs from 'node:fs';\nimport path from 'node:path';\n\nconst PORT = process.env.PORT || 3000;\nconst DATA_DIR = process.env.DATA_DIR || path.join(process.cwd(), '.data');\nconst AUDIT_FILE = path.join(DATA_DIR, 'audit.jsonl');\nconst STATE_FILE = path.join(DATA_DIR, 'state.json');\nconst RETENTION_DAYS = Number(process.env.AUDIT_RETENTION_DAYS || 365);\n\nfs.mkdirSync(DATA_DIR, { recursive: true });\nif (!fs.existsSync(AUDIT_FILE)) fs.writeFileSync(AUDIT_FILE, '');\n\nconst state = loadState();\n\nconst config = {\n  autonomy: {\n    level: process.env.AUTONOMY_LEVEL || 'HIL', // SUGGEST_ONLY | HIL | HOL\n    requireApprovalFor: ['production_change', 'merge_main', 'release']\n  },\n  oidc: {\n    issuer: process.env.OIDC_ISSUER || 'https://issuer.example.com',\n    audience: process.env.OIDC_AUDIENCE || 'fde-sdlc-control-plane',\n    // For MVP: HS256-like shared secret fallback OR RS256 public key\n    sharedSecret: process.env.OIDC_SHARED_SECRET || 'dev-secret',\n    publicKeyPem: process.env.OIDC_PUBLIC_KEY_PEM || ''\n  },\n  qualityGates: {\n    minPassRate: Number(process.env.GATE_MIN_PASS_RATE || 0.9),\n    requireSecurityScanPass: true,\n    maxRiskScore: Number(process.env.GATE_MAX_RISK || 0.4),\n    minApprovals: Number(process.env.GATE_MIN_APPROVALS || 1)\n  },\n  modelProvider: {\n    enabled: process.env.MODEL_PROVIDER_ENABLED !== 'false'\n  }\n};\n\nconst roles = {\n  admin: ['*'],\n  product_manager: ['initiative:write', 'initiative:read', 'dashboard:read', 'kpi:write', 'kpi:read'],\n  engineer: ['initiative:read', 'action:write', 'action:read', 'test:write', 'test:read', 'release:read', 'dashboard:read'],\n  qa: ['test:write', 'test:read', 'release:write', 'release:read', 'dashboard:read'],\n  approver: ['approval:write', 'action:read', 'release:read', 'dashboard:read'],\n  auditor: ['audit:read', 'dashboard:read', 'kpi:read']\n};\n\nconst adapters = {\n  workTracker: createWorkTrackerAdapter(),\n  scm: createScmAdapter(),\n  ci: createCiAdapter(),\n  itsm: createItsmAdapter(),\n  collaboration: createCollabAdapter(),\n  observability: createObservabilityAdapter(),\n  model: createModelAdapter()\n};\n\nconst server = http.createServer(async (req, res) => {\n  try {\n    const url = new URL(req.url, `http://${req.headers.host}`);\n    const body = await readBody(req);\n    const user = authenticate(req);\n\n    if (req.method === 'GET' && url.pathname === '/health') {\n      return send(res, 200, { ok: true, uptime: process.uptime(), modelProvider: config.modelProvider.enabled ? 'enabled' : 'degraded' });\n    }\n\n    if (req.method === 'POST' && url.pathname === '/initiatives') {\n      authorize(user, 'initiative:write');\n      const { title, description, team = 'pilot-team' } = body;\n      if (!title || !description) return send(res, 400, { error: 'title and description required' });\n\n      const decomposition = await adapters.model.decomposeInitiative({ title, description });\n      const initiative = {\n        id: randomUUID(),\n        title,\n        description,\n        team,\n        decomposition,\n        createdAt: new Date().toISOString(),\n        createdBy: user.sub\n      };\n      state.initiatives.push(initiative);\n\n      const syncResult = await adapters.workTracker.syncBacklog(initiative);\n      initiative.workTracker = syncResult;\n\n      appendAudit({\n        actor: user.sub,\n        role: user.role,\n        action: 'initiative.create',\n        why: 'Natural language initiative submitted and decomposed',\n        evidence: { initiativeId: initiative.id, workTracker: syncResult }\n      });\n\n      persistState();\n      return send(res, 201, initiative);\n    }\n\n    if (req.method === 'POST' && url.pathname === '/actions/plan') {\n      authorize(user, 'action:write');\n      const { initiativeId, actionType, payload = {}, environment = 'dev' } = body;\n      const initiative = state.initiatives.find(i => i.id === initiativeId);\n      if (!initiative) return send(res, 404, { error: 'initiative not found' });\n\n      const decision = evaluatePolicy({ actionType, environment, user, autonomyLevel: config.autonomy.level });\n      const action = {\n        id: randomUUID(),\n        initiativeId,\n        actionType,\n        payload,\n        environment,\n        decision,\n        status: decision.allowed && !decision.requiresApproval ? 'approved' : (decision.requiresApproval ? 'pending_approval' : 'blocked'),\n        createdAt: new Date().toISOString(),\n        createdBy: user.sub,\n        reversibleBy: null,\n        execution: null\n      };\n      state.actions.push(action);\n\n      appendAudit({ actor: user.sub, role: user.role, action: 'action.plan', why: decision.reason, evidence: { actionId: action.id, decision } });\n      persistState();\n      return send(res, 201, action);\n    }\n\n    if (req.method === 'POST' && url.pathname === '/actions/approve') {\n      authorize(user, 'approval:write');\n      const { actionId, approve = true, reason = '' } = body;\n      const action = state.actions.find(a => a.id === actionId);\n      if (!action) return send(res, 404, { error: 'action not found' });\n      if (action.status !== 'pending_approval') return send(res, 400, { error: 'action not pending approval' });\n\n      action.status = approve ? 'approved' : 'rejected';\n      action.approval = { approver: user.sub, approvedAt: new Date().toISOString(), reason, approve };\n\n      appendAudit({ actor: user.sub, role: user.role, action: 'action.approve', why: reason || 'approval decision', evidence: { actionId, approve } });\n      persistState();\n      return send(res, 200, action);\n    }\n\n    if (req.method === 'POST' && url.pathname === '/actions/execute') {\n      authorize(user, 'action:write');\n      const { actionId } = body;\n      const action = state.actions.find(a => a.id === actionId);\n      if (!action) return send(res, 404, { error: 'action not found' });\n      if (action.status !== 'approved') return send(res, 400, { error: 'action not approved' });\n\n      const result = await executeAction(action);\n      action.status = 'executed';\n      action.execution = result;\n      action.reversibleBy = user.sub;\n\n      appendAudit({ actor: user.sub, role: user.role, action: 'action.execute', why: 'approved action execution', evidence: { actionId, result } });\n      persistState();\n      return send(res, 200, action);\n    }\n\n    if (req.method === 'POST' && url.pathname === '/actions/reverse') {\n      authorize(user, 'action:write');\n      const { actionId, reason = 'rollback requested' } = body;\n      const action = state.actions.find(a => a.id === actionId);\n      if (!action) return send(res, 404, { error: 'action not found' });\n      if (action.status !== 'executed') return send(res, 400, { error: 'only executed actions can be reversed' });\n\n      const rollback = await reverseAction(action);\n      action.status = 'reversed';\n      action.rollback = rollback;\n\n      appendAudit({ actor: user.sub, role: user.role, action: 'action.reverse', why: reason, evidence: { actionId, rollback } });\n      persistState();\n      return send(res, 200, action);\n    }\n\n    if (req.method === 'POST' && url.pathname === '/tests/generate-and-run') {\n      authorize(user, 'test:write');\n      const { initiativeId, suite = ['unit', 'integration', 'regression'] } = body;\n      const initiative = state.initiatives.find(i => i.id === initiativeId);\n      if (!initiative) return send(res, 404, { error: 'initiative not found' });\n\n      const job = {\n        id: randomUUID(),\n        type: 'test-run',\n        status: 'queued',\n        createdAt: new Date().toISOString(),\n        createdBy: user.sub,\n        initiativeId,\n        suite,\n        result: null\n      };\n      state.jobs.push(job);\n      persistState();\n\n      runAsync(async () => {\n        job.status = 'running';\n        const plan = await adapters.model.generateTestPlan({ initiative, suite });\n        const ciResult = await adapters.ci.publishAndRunTests({ initiative, plan });\n        job.result = { plan, ciResult };\n        job.status = 'completed';\n\n        appendAudit({ actor: user.sub, role: user.role, action: 'tests.generate_run', why: 'automated test orchestration', evidence: { jobId: job.id, ciRunId: ciResult.runId } });\n        persistState();\n      });\n\n      return send(res, 202, { jobId: job.id, status: job.status });\n    }\n\n    if (req.method === 'GET' && url.pathname.startsWith('/jobs/')) {\n      authorize(user, 'test:read');\n      const id = url.pathname.split('/')[2];\n      const job = state.jobs.find(j => j.id === id);\n      if (!job) return send(res, 404, { error: 'job not found' });\n      return send(res, 200, job);\n    }\n\n    if (req.method === 'POST' && url.pathname === '/release-readiness/evaluate') {\n      authorize(user, 'release:write');\n      const { initiativeId, securityScanPassed = true, riskScore = 0.2, approvals = 1 } = body;\n      const latestJob = [...state.jobs].reverse().find(j => j.initiativeId === initiativeId && j.type === 'test-run' && j.status === 'completed');\n      if (!latestJob) return send(res, 400, { error: 'no completed test run for initiative' });\n\n      const passRate = latestJob.result.ciResult.passRate;\n      const gates = config.qualityGates;\n      const passed = passRate >= gates.minPassRate && (!gates.requireSecurityScanPass || securityScanPassed) && riskScore <= gates.maxRiskScore && approvals >= gates.minApprovals;\n\n      const evaluation = {\n        id: randomUUID(),\n        initiativeId,\n        passRate,\n        securityScanPassed,\n        riskScore,\n        approvals,\n        gates,\n        passed,\n        evaluatedAt: new Date().toISOString()\n      };\n      state.releaseEvaluations.push(evaluation);\n\n      appendAudit({ actor: user.sub, role: user.role, action: 'release.evaluate', why: 'quality gate evaluation', evidence: evaluation });\n      persistState();\n      return send(res, 200, evaluation);\n    }\n\n    if (req.method === 'GET' && url.pathname === '/dashboards/role') {\n      authorize(user, 'dashboard:read');\n      const role = url.searchParams.get('role') || user.role;\n      const dashboard = buildDashboard(role);\n      return send(res, 200, dashboard);\n    }\n\n    if (req.method === 'POST' && url.pathname === '/kpi/baseline') {\n      authorize(user, 'kpi:write');\n      const { leadTimeDays, deploysPerWeek, sev1 = 0, sev2 = 0 } = body;\n      state.kpi.baseline = { leadTimeDays, deploysPerWeek, sev1, sev2, at: new Date().toISOString() };\n      persistState();\n      return send(res, 200, state.kpi.baseline);\n    }\n\n    if (req.method === 'POST' && url.pathname === '/kpi/current') {\n      authorize(user, 'kpi:write');\n      const { leadTimeDays, deploysPerWeek, sev1 = 0, sev2 = 0 } = body;\n      state.kpi.current = { leadTimeDays, deploysPerWeek, sev1, sev2, at: new Date().toISOString() };\n      persistState();\n      return send(res, 200, state.kpi.current);\n    }\n\n    if (req.method === 'GET' && url.pathname === '/kpi/evaluate-90d') {\n      authorize(user, 'kpi:read');\n      const b = state.kpi.baseline;\n      const c = state.kpi.current;\n      if (!b || !c) return send(res, 400, { error: 'baseline and current required' });\n      const leadTimeReduction = (b.leadTimeDays - c.leadTimeDays) / b.leadTimeDays;\n      const deployIncrease = (c.deploysPerWeek - b.deploysPerWeek) / b.deploysPerWeek;\n      const sevGuardrail = c.sev1 <= b.sev1 && c.sev2 <= b.sev2;\n      return send(res, 200, {\n        leadTimeReduction,\n        deployIncrease,\n        sevGuardrail,\n        passed: leadTimeReduction >= 0.2 && deployIncrease >= 0.15 && sevGuardrail\n      });\n    }\n\n    if (req.method === 'GET' && url.pathname === '/audit/export') {\n      authorize(user, 'audit:read');\n      const lines = fs.readFileSync(AUDIT_FILE, 'utf8').trim().split('\\n').filter(Boolean).map(l => JSON.parse(l));\n      return send(res, 200, { retentionDays: RETENTION_DAYS, count: lines.length, records: lines });\n    }\n\n    if (req.method === 'GET' && url.pathname === '/audit/verify') {\n      authorize(user, 'audit:read');\n      const lines = fs.readFileSync(AUDIT_FILE, 'utf8').trim().split('\\n').filter(Boolean).map(l => JSON.parse(l));\n      let prev = 'GENESIS';\n      for (const rec of lines) {\n        const expected = hashRecord(rec.entry, prev);\n        if (expected !== rec.hash || rec.prevHash !== prev) {\n          return send(res, 200, { valid: false, brokenAt: rec.entry.id });\n        }\n        prev = rec.hash;\n      }\n      return send(res, 200, { valid: true, count: lines.length });\n    }\n\n    if (req.method === 'GET' && url.pathname === '/integrations/status') {\n      authorize(user, 'dashboard:read');\n      const status = await Promise.all(Object.entries(adapters).map(async ([name, adapter]) => ({ name, status: await adapter.status() })));\n      return send(res, 200, status);\n    }\n\n    return send(res, 404, { error: 'not found' });\n  } catch (err) {\n    return send(res, err.statusCode || 500, { error: err.message || 'internal error' });\n  }\n});\n\nserver.listen(PORT, () => {\n  console.log(`FDE SDLC control plane listening on :${PORT}`);\n});\n\nfunction loadState() {\n  if (fs.existsSync(STATE_FILE)) return JSON.parse(fs.readFileSync(STATE_FILE, 'utf8'));\n  return {\n    initiatives: [],\n    actions: [],\n    jobs: [],\n    releaseEvaluations: [],\n    kpi: { baseline: null, current: null }\n  };\n}\n\nfunction persistState() {\n  fs.writeFileSync(STATE_FILE, JSON.stringify(state, null, 2));\n}\n\nfunction readBody(req) {\n  return new Promise((resolve) => {\n    let data = '';\n    req.on('data', c => data += c);\n    req.on('end', () => {\n      if (!data) return resolve({});\n      try { resolve(JSON.parse(data)); } catch